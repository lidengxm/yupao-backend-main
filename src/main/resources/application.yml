spring:
  application:
    name: usercentre
  # DataSource Config
  datasource:
    driver-class-name: com.mysql.jdbc.Driver
    url: jdbc:mysql://localhost:3306/usercentre
    username: root
    password: hsp
server:
  port: 8080
